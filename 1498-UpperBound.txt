  int searchMaxNumber(int start, int end,vector<int>& nums, int target){// target should be minused
    // need to check the boundary of last number
        vector<int>::iterator upper = upper_bound(nums.begin()+start , nums.end(),target );
        int upperIndex = upper - nums.begin() -1;
        if(nums[upperIndex] > target || upperIndex < start) return -1;
        return upperIndex;
    }